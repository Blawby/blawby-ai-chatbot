# Local development environment variables (Worker Secrets Only)
# Copy this file to .dev.vars and fill in your actual values
# 
# Note: Non-sensitive configuration variables (like AI_MODEL_DEFAULT, ENABLE_WORKERS_AI, etc.)
# are now managed in wrangler.toml instead of this file.
# 
# Updated for Better Auth organization migration - all "team" references changed to "organization"
#
# Frontend Environment Variables (Vite):
# - VITE_API_URL: Set to 'http://localhost:8787' for local development (automatically set by vite.config.ts)
# - In production, VITE_API_URL is undefined, so the frontend uses relative URLs (/api/...)

# API Keys and Tokens (SENSITIVE)
BLAWBY_API_TOKEN=your_blawby_api_token_here
LAWYER_SEARCH_API_KEY=your_lawyer_search_api_key_here
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
RESEND_API_KEY=your_resend_api_key_here

# Account IDs (SENSITIVE)
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here

# Adobe PDF Services API Configuration (SENSITIVE)
ADOBE_CLIENT_ID=your_adobe_client_id_here
ADOBE_CLIENT_SECRET=your_adobe_client_secret_here
ADOBE_TECHNICAL_ACCOUNT_ID=your_technical_account_id@techacct.adobe.com
ADOBE_TECHNICAL_ACCOUNT_EMAIL=your_technical_account_email@techacct.adobe.com
ADOBE_ORGANIZATION_ID=your_organization_id@AdobeOrg

# Remote Auth Server Configuration (SENSITIVE)
# Note: Auth is now handled by remote server. These are kept for legacy compatibility.
AUTH_SERVER_URL=https://staging-api.blawby.com
# Legacy Better Auth config (deprecated - kept for migration period)
BETTER_AUTH_SECRET=your_better_auth_secret_here
BETTER_AUTH_URL=http://localhost:8787

# Frontend Environment Variables (Vite)
# These should be set in your .env file or build environment, not in .dev.vars
# .dev.vars is for Worker secrets only, but documented here for reference:
# 
# VITE_AUTH_SERVER_URL: URL of your Better Auth server
#   - Required in production
#   - Optional in development (falls back to ngrok URL for local testing)
#   - Example: https://auth.yourdomain.com or http://localhost:8787

# Better Auth Feature Flags (NON-SENSITIVE)
# Set to 'true' to enable geolocation and IP detection features
# Note: These features require proper Cloudflare context and are disabled by default
ENABLE_AUTH_GEOLOCATION=false
ENABLE_AUTH_IP_DETECTION=false

# Domain Configuration (NON-SENSITIVE)
# DOMAIN=localhost  # Set this in .dev.vars for local development

# Google OAuth Configuration (SENSITIVE)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Stripe Configuration (SENSITIVE)
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here
STRIPE_CONNECT_WEBHOOK_SECRET=your_stripe_connect_webhook_secret_here
STRIPE_PRICE_ID=your_price_id_here
STRIPE_ANNUAL_PRICE_ID=your_annual_price_id_here

# Development Practice Seeding (for npm run dev:seed script)
# These are used by the dev-seed-practice.ts script to automatically create a practice
# Note: These are Node.js environment variables, not Worker secrets
# Set these in your shell or .env file, or export them before running npm run dev:seed
# DEV_SEED_BASE_URL=http://localhost:8787  # Worker URL for practice API calls
# DEV_SEED_AUTH_URL=https://staging-api.blawby.com  # Remote auth server URL for sign-in
# DEV_SEED_USER_EMAIL=your-dev-email@example.com
# DEV_SEED_USER_PASSWORD=your-dev-password
# DEV_SEED_PRACTICE_NAME=Dev Practice
# DEV_SEED_PRACTICE_SLUG=dev-practice
