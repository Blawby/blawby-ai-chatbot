import { InboxPage } from '@/features/settings/pages/InboxPage';

export const MockInboxPage = () => {
  return (
    <div className="h-screen bg-white dark:bg-dark-bg">
      <InboxPage className="h-full" />
    </div>
  );
};

export default MockInboxPage;
